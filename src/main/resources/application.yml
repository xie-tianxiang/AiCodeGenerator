spring:
  application:
    name: AiCodeGenerator
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/aicodegenerator
    username: root
    password: 0123456xuanxuan
  profiles:
    active: local
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.xtx.aicodegenerator.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn